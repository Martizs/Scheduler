import { StyleSheet } from 'react-native';

export const menuStyle = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
    alignItems: 'center',
  },
  list: {
    width: '97%',
  },
});
