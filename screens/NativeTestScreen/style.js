import { StyleSheet } from 'react-native';
import { textContainer } from './../../styles/generalStyles';

export const natTest = StyleSheet.create({
  mainText: {
    ...textContainer.style,
    marginTop: 'auto',
    marginBottom: 'auto',
  },
});
