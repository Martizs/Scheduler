import {StyleSheet} from 'react-native';
/* styles */
import {titleDivHeight} from '../../../../styles/theme';

export const VerBarStyle = StyleSheet.create({
  container: {
    height: '100%',
    width: titleDivHeight,
  },
  luminance: {
    marginTop: 'auto',
    marginBottom: 'auto',
    height: '98%',
    width: '100%',
    borderRadius: 10,
  },
});
