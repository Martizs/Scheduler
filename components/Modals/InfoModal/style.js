import {StyleSheet} from 'react-native';
/* styles */
import {textContainer} from '../../../styles/generalStyles';

export const infoContent = StyleSheet.create({
  text: {
    ...textContainer.style,
    paddingBottom: 10,
    paddingTop: 10,
  },
});
